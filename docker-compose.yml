version: '3.8'

services:
  pigi:
    build: .               # Aynı klasördeki Dockerfile'ı kullan
    container_name: pigi
    ports:
      - "8888:8888"
    environment:
      - API_PASSWORD=Marco123   # ← BURAYI KENDİ GÜÇLÜ ŞİFRENLE DEĞİŞTİR!
    restart: unless-stopped
